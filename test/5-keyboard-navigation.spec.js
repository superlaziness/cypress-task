describe('5 Keyboard navigation', () => {
    beforeEach(() => {
        cy.visit('https://www.jetbrains.com/help/idea/installation-guide.html');
    });

    it('1 Tab keyboard button should select next node in menu', () => {});

    it('2 Enter keyboard button should select node and open content', () => {});

    xit('3 Menu should be scrolled when bottom node in visible area is selected', () => {});
});
